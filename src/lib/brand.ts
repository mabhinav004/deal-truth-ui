/** Canonical user-facing product name. */
export const PRODUCT_NAME = "Sach.AI";

/** Fallback prefix for client-generated request ids (not a secret). */
export const REQUEST_ID_PREFIX = "sach-ai";

/** localStorage key for HubSpot/Slack action preferences. */
export const INTEGRATION_PREFS_KEY = "sach.ai.integrations.prefs";
